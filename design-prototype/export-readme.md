# AI Mac Cleaner 高保真原型 · 导出交付说明

> 交付物版本：v1.4.0 界面原型
> 导出角色：设计原型专家团 · 导出交付专家（交付达）
> 交付形态：**单一自包含 HTML 文件**（零外部依赖）

---

## 一、交付清单

| 文件 | 大小 | 说明 |
|------|------|------|
| `index.html` | ~97 KB | **主交付物**，可独立运行的高保真原型 |
| `design-tokens.md` | ~31 KB | 设计令牌唯一真源（色彩 / 字体 / 间距 / 阴影 / 圆角） |
| `requirement-summary.md` | ~5.7 KB | 需求摘要与功能边界 |
| `review-report.md` | ~8 KB | 质量审查报告（已通过） |
| `export-readme.md` | 本文件 | 导出与使用说明 |
| `expunge-design-prototype.zip` | — | 以上全部文件的打包版本 |

---

## 二、打开方式

### 方式 1：双击打开（推荐）

在访达（Finder）中找到 `index.html`，**双击**即可用默认浏览器打开。

无需安装任何依赖，无需启动本地服务器，无需联网。

### 方式 2：指定浏览器打开

在 `index.html` 上点右键 → **打开方式** → 选择 Safari / Chrome / Edge。

### 方式 3：终端命令

```bash
open "/Users/hwd7500/Documents/ai-mac-cleaner/design-prototype/index.html"
```

### 从 ZIP 解压后使用

双击 `expunge-design-prototype.zip` 解压，进入解压出的文件夹，双击其中的 `index.html` 即可。
四个 Markdown 文档与 HTML 位于同级目录，**不影响 HTML 独立运行**——即使只把 `index.html` 单独拷走，也能完整显示。

---

## 三、原型可交互内容

这是**纯前端视觉原型**，不含任何真实业务逻辑，不会读写你的磁盘，不会删除任何文件。

| 区域 | 可操作项 |
|------|---------|
| **侧边栏三 Tab** | 「应用卸载 / 残留清理 / 历史记录」自由切换 |
| **应用列表** | 点击应用条目切换选中态；搜索框可聚焦输入 |
| **痕迹清单** | 复选框勾选 / 取消；分组标题支持展开收起与半选态（indeterminate） |
| **底部操作栏** | 选中计数与体积统计随勾选实时联动 |
| **确认弹窗** | 点击「卸载」「删除所选」唤起二次确认弹窗；支持遮罩点击、Esc 键、取消按钮关闭 |
| **结果弹窗** | 展示卸载结果摘要与逐项日志 |
| **复制按钮** | 「复制全部路径 / 复制全部日志」点击后有 1.4 秒成功反馈动效 |
| **风险标签** | 安全 / 用户数据 / 不确定 三色语义标识 |

**建议浏览环境**：窗口宽度 ≥ 1280px，以完整还原 macOS 窗口质感（毛玻璃标题栏、侧边栏半透明、发丝级分隔线、层叠阴影）。

---

## 四、独立化校验结论

对 `index.html` 逐项审计，**全部达标，零外部资源请求**：

| # | 校验项 | 结果 | 实测证据 |
|---|--------|------|---------|
| 1 | 无 `http://` / `https://` 引用 | ✅ 通过 | 全文匹配 `https?://` 命中 **0** 处 |
| 2 | 无外部样式表 `<link rel=stylesheet>` | ✅ 通过 | 全文 `<link>` 标签 **0** 个 |
| 3 | 无外部脚本 `<script src>` | ✅ 通过 | 唯一 `<script>` 为内联，无 `src` 属性 |
| 4 | 无 `@import` 外链 | ✅ 通过 | 命中 **0** 处 |
| 5 | 无外部字体 CDN / `@font-face` | ✅ 通过 | 命中 **0** 处；字体全部走系统栈 |
| 6 | 无外部图片 `<img src>` | ✅ 通过 | `<img>` 标签 **0** 个 |
| 7 | 无 `<iframe>` / `<object>` / `<embed>` | ✅ 通过 | 命中 **0** 处 |
| 8 | 无 `integrity` / `crossorigin`（SRI 外链特征） | ✅ 通过 | 命中 **0** 处 |
| 9 | CSS 全部内联 | ✅ 通过 | 单个 `<style>` 块，34,624 字符，`{`/`}` 各 231 配对 |
| 10 | JS 全部内联 | ✅ 通过 | 单个 `<script>` 块，9,061 字符，`node --check` 语法通过 |
| 11 | 图标全部内联 | ✅ 通过 | 24 个 `<symbol>` 组成内联 SVG sprite，101 个 `<svg>` 开闭配对 |
| 12 | 图标引用完整性 | ✅ 通过 | 21 处 `use href="#..."` 引用**全部命中**已定义 symbol，无缺失 |
| 13 | 无 base64 / `data:` URI 冗余 | ✅ 通过 | 真实 data URI **0** 个（图标为矢量 path，非位图内嵌） |
| 14 | HTML 结构完整 | ✅ 通过 | `html` / `head` / `body` / `style` / `script` 标签均 1 开 1 闭 |

### 字体策略说明

原型未下载任何网络字体，使用纯系统字体栈：

- **UI 字体**：`-apple-system, BlinkMacSystemFont, "SF Pro Text", "SF Pro Display", …`
- **等宽字体**：`ui-monospace, "SF Mono", SFMono-Regular, Menlo, Consolas, …`

在 macOS 上会自动命中 SF Pro / SF Mono，呈现原生质感；在 Windows / Linux 上优雅降级到系统默认字体，布局不塌陷。

### HTML 规范性

源文件**已完全符合规范，本次导出未做任何修改**：

```html
<!DOCTYPE html>          ✅ 已有
<html lang="zh-CN">      ✅ 已有
<meta charset="UTF-8">   ✅ 已有
<meta name="viewport" …> ✅ 已有
<title>AI Mac Cleaner — 界面原型</title>  ✅ 已有
```

### 一处非阻断提示（不影响使用）

SVG sprite 中有 3 个已定义但当前未被引用的备用图标：`i-disk`、`i-failf`、`i-okf`，合计约 0.7 KB。它们属于图标库预留资产，**不产生任何网络请求，不影响渲染与性能**，故保留原样未做删减。

---

## 五、注意事项

- **视觉零改动**：本次导出仅做交付层校验与打包，**未修改任何设计令牌、样式数值或 DOM 结构**。
- **浏览器兼容**：推荐 Safari 16+ / Chrome 110+ / Edge 110+。原型使用了 `backdrop-filter`（毛玻璃）、CSS 自定义属性、`:has()` 等现代特性，老旧浏览器（如 IE、Chrome 80 以下）会有降级表现。
- **深浅色**：当前交付为 **Light 模式**单一主题，未包含 Dark 模式变体。
- **离线可用**：断网状态下功能与联网时完全一致。
- **可安全分发**：单文件可直接通过邮件、IM、云盘传输，接收方双击即可查看，无需还原任何工程环境。
- **非可运行软件**：这是界面原型（Prototype），不是 AI Mac Cleaner 应用本身，所有数据均为演示用的静态假数据。

---

## 六、二次开发提示

若需在此原型基础上继续开发：

1. **改样式先改 token**——所有颜色 / 字号 / 间距 / 圆角 / 阴影集中定义在 `<style>` 开头的 `:root` 块中，与 `design-tokens.md` 一一对应。改 `:root` 变量即可全局换肤，不要散改具体规则。
2. **加图标**——在 `<svg id="sprite">` 内新增 `<symbol id="i-xxx" viewBox="0 0 24 24">`，然后用 `<svg class="ic ic-12"><use href="#i-xxx"/></svg>` 引用，保持零外链原则。
3. **接真实数据**——JS 底部的 `syncAll()` 为初始化入口，选中态与统计联动逻辑集中在此，替换数据源时从这里切入。
